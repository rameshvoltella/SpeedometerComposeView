package com.ramzmania.speedometercomposeview

/**
 * Enum class representing different types of gradients for drawing arcs in gradient mode.
 */
enum class GradientType {
    LINEAR,
    HORIZONTAL,
    VERTICAL,
    RADIAL,
    SWEEP
}